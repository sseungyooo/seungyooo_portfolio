import { supabase } from './supabase-config.js';
const defaults = {projectTitle0:'Ovoid / Wellness',projectDetail0:'Brand identity · 2025',projectTitle1:'Juun / Editorial',projectDetail1:'Art direction · 2025',projectTitle2:'Objects in form',projectDetail2:'Digital experience · 2024',projectTitle3:'Visual system',projectDetail3:'Brand experience · 2024',projectTitle4:'Archive edition',projectDetail4:'Editorial design · 2024',projectTitle5:'New object',projectDetail5:'Digital product · 2024'};
const colors=['#ccb9f8','#fd7e58','#172426','#c9e7d4','#f6dfc5','#b7c5ff'];
function render(content){const grid=document.querySelector('#allWorkGrid');grid.innerHTML=Array.from({length:6},(_,i)=>{const image=content[`projectImage${i}`];const style=image?`background:center / cover no-repeat url('${image}')`:`background:${colors[i]}`;return `<article class="all-work-card"><div class="project-art" style="${style}"><span>${String(i+1).padStart(2,'0')}</span></div><div class="project-meta"><h2>${content[`projectTitle${i}`]}</h2><p>${content[`projectDetail${i}`]}</p></div></article>`}).join('');}
render(defaults);const {data}=await supabase.from('site_content').select('content').eq('id','main').maybeSingle();if(data?.content)render({...defaults,...data.content});
const grid=document.querySelector('#allWorkGrid');grid.addEventListener('wheel',event=>{if(Math.abs(event.deltaY)>Math.abs(event.deltaX)){event.preventDefault();grid.scrollLeft+=event.deltaY;}},{passive:false});
